image: atlassian/default-image:latest

definitions:
  services:
    docker:
      memory: 2048
  caches:
    node-custom: node_modules
    sonar: ~/.sonar

clone:
  depth: full

pipelines:
  branches:
    main:
      - parallel:
          - step: &lint-step
              name: Lint and Style Check
              image: node:22.18.0
              caches:
                - node-custom
              script:
                - npm ci
                - npm run style-check
                - npm run lint
          - step: &sonarqube
              name: sonarqube
              image: sonarsource/sonar-scanner-cli:12
              runs-on:
                - self.hosted
                - linux
                - operation.internal
              caches:
                - sonar
              script:
                - sonar-scanner
                  -Dsonar.host.url=${SONAR_HOST_URL}
                  -Dsonar.token=${SONAR_TOKEN}
                  -Dsonar.projectKey=OmniCam

  pull-requests:
    "**":
      - parallel:
          - step:
              <<: *lint-step

  tags:
    "*":
      - parallel:
          - step: &build-push-frontend
              name: Build & Push Frontend
              services:
                - docker
              script:
                - export DOCKER_BUILDKIT=1
                - echo "$DOCKER_HUB_ACCESS_TOKEN" | docker login docker.io -u "$DOCKER_HUB_USER" --password-stdin
                - FINAL_TAG="${BITBUCKET_TAG:-${MANUAL_TAG:-manual-build}}"
                - docker build -t docker.io/oxygenai/omnicam-frontend:${FINAL_TAG} -t docker.io/oxygenai/omnicam-frontend:latest -f ./frontend/Dockerfile .
                - docker push docker.io/oxygenai/omnicam-frontend:${FINAL_TAG}
                - docker push docker.io/oxygenai/omnicam-frontend:latest

          - step: &build-push-backend
              name: Build & Push Backend
              services:
                - docker
              script:
                - export DOCKER_BUILDKIT=1
                - echo "$DOCKER_HUB_ACCESS_TOKEN" | docker login docker.io -u "$DOCKER_HUB_USER" --password-stdin
                - FINAL_TAG="${BITBUCKET_TAG:-${MANUAL_TAG:-manual-build}}"
                - docker build -t docker.io/oxygenai/omnicam-backend:${FINAL_TAG} -t docker.io/oxygenai/omnicam-backend:latest -f ./backend/Dockerfile .
                - docker push docker.io/oxygenai/omnicam-backend:${FINAL_TAG}
                - docker push docker.io/oxygenai/omnicam-backend:latest

          - step: &build-push-algo
              name: Build & Push Algo
              services:
                - docker
              script:
                - export DOCKER_BUILDKIT=1
                - echo "$DOCKER_HUB_ACCESS_TOKEN" | docker login docker.io -u "$DOCKER_HUB_USER" --password-stdin
                - FINAL_TAG="${BITBUCKET_TAG:-${MANUAL_TAG:-manual-build}}"
                - docker build -t docker.io/oxygenai/omnicam-algo:${FINAL_TAG} -t docker.io/oxygenai/omnicam-algo:latest -f ./predictive-algorithm/Dockerfile .
                - docker push docker.io/oxygenai/omnicam-algo:${FINAL_TAG}
                - docker push docker.io/oxygenai/omnicam-algo:latest

          - step: &build-push-data-migration
              name: Build & Push Data Migration
              services:
                - docker
              script:
                - export DOCKER_BUILDKIT=1
                - echo "$DOCKER_HUB_ACCESS_TOKEN" | docker login docker.io -u "$DOCKER_HUB_USER" --password-stdin
                - FINAL_TAG="${BITBUCKET_TAG:-${MANUAL_TAG:-manual-build}}"
                - docker build -t docker.io/oxygenai/omnicam-data-migration:${FINAL_TAG} -t docker.io/oxygenai/omnicam-data-migration:latest -f ./db/Dockerfile .
                - docker push docker.io/oxygenai/omnicam-data-migration:${FINAL_TAG}
                - docker push docker.io/oxygenai/omnicam-data-migration:latest

  custom:
    manual-release-build:
      - variables:
          - name: MANUAL_TAG
            default: "manual-build"
            description: "Custom tag for the image"
      - parallel:
          - step: *build-push-algo
          - step: *build-push-frontend
          - step: *build-push-backend
          - step: *build-push-data-migration
